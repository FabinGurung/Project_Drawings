const DB=window.__STAGE_DATA__;


const rows=Object.fromEntries(DB.rows.map(r=>[r.structure_id,r]));
const F={
 drawing_rcc_volume_m3:{label:'RCC volume',unit:'m³',digits:4},
 cement_bags:{label:'Cement',unit:'bags',digits:4},
 sand_m3:{label:'Sand',unit:'m³',digits:5},
 aggregate_m3:{label:'Aggregate',unit:'m³',digits:5}
};
function fmt(v,n=3){return Number(v).toLocaleString(undefined,{minimumFractionDigits:n,maximumFractionDigits:n})}
function showPage(id,btn){document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));document.getElementById('page-'+id).classList.add('active');document.querySelectorAll('.tabbtn').forEach(x=>x.classList.remove('active'));btn.classList.add('active');closePop();window.scrollTo({top:0,behavior:'smooth'})}
function closePop(){document.getElementById('pop').classList.remove('open');document.querySelectorAll('.rccFoot').forEach(e=>e.classList.remove('sel'))}
function openRcc(ev,el){ev.stopPropagation();document.querySelectorAll('.rccFoot').forEach(e=>e.classList.remove('sel'));el.classList.add('sel');const r=rows[el.dataset.id];document.getElementById('pc').innerHTML=`<h3>${r.structure_type} RCC · ${r.grid_ref}</h3><div class="prov"><b>${r.rcc_spec}</b><br>Support: ${r.support_types.join(' + ')}<br>Current: ${r.current_basis}<br>Comparison: ${r.comparison_basis}</div><div class="metrics"><div class="m"><b>${fmt(r.drawing_rcc_volume_m3,4)} m³</b><span>RCC — this footing</span></div><div class="m"><b>${fmt(r.cement_bags,4)} bags</b><span>Cement — this footing</span></div><div class="m"><b>${fmt(r.sand_m3,5)} m³</b><span>Sand — this footing</span></div><div class="m"><b>${fmt(r.aggregate_m3,5)} m³</b><span>Aggregate — this footing</span></div></div><div class="prov">Report-depth comparison RCC: ${fmt(r.report_comparison_rcc_volume_m3,4)} m³ — comparison only, not mixed into current totals.<br><br><b>Database row:</b> ${r.structure_id}<br>${r.derivation_method}</div>`;const p=document.getElementById('pop');p.classList.add('open');p.style.left=Math.max(12,Math.min(ev.clientX+12,window.innerWidth-470))+'px';p.style.top=Math.max(70,Math.min(ev.clientY+12,window.innerHeight-470))+'px'}
function selected(selector){return [...document.querySelectorAll(selector+':checked')].map(x=>x.value)}
function typeOf(id){return rows[id].structure_type}
function setStructures(mode){document.querySelectorAll('.structureCheck').forEach(cb=>cb.checked=mode==='ALL'||(mode!=='NONE'&&typeOf(cb.value)===mode));refreshSelector()}
function filterStructures(){const q=document.getElementById('structureSearch').value.toLowerCase();document.querySelectorAll('#structureList .checkrow').forEach(e=>e.style.display=e.dataset.search.includes(q)?'flex':'none')}
function syncMap(panelId,selector,idList){const set=new Set(idList),panel=document.getElementById(panelId);panel.classList.toggle('has-selection',idList.length>0);document.querySelectorAll(selector+' .rccFoot').forEach(e=>e.classList.toggle('selectedMap',set.has(e.dataset.id)))}
function toggleSelectorMap(ev,el){ev.stopPropagation();const cb=document.querySelector(`.structureCheck[value="${el.dataset.id}"]`);if(cb){cb.checked=!cb.checked;refreshSelector()}}
function refreshSelector(){const ids=selected('.structureCheck'),fields=selected('.fieldCheck');syncMap('selectorMapPanel','#selectorMapPanel',ids);document.getElementById('selectorStatus').textContent=ids.length?`${ids.length} selected • spatial highlight active`:'0 selected';document.getElementById('selectedCount').textContent=ids.length;document.getElementById('selectedTypes').textContent=[...new Set(ids.map(typeOf))].join(', ')||'—';document.getElementById('selectedChips').innerHTML=ids.slice(0,12).map(id=>`<span class="chip">${rows[id].structure_type} · ${rows[id].grid_ref}</span>`).join('');const has=ids.length&&fields.length;document.getElementById('summaryEmpty').style.display=has?'none':'block';document.getElementById('summaryArea').style.display=has?'block':'none';if(!has)return;const sums={};fields.forEach(k=>sums[k]=ids.reduce((a,id)=>a+Number(rows[id][k]),0));document.getElementById('sumBody').innerHTML=fields.map(k=>`<tr><td>${F[k].label}</td><td>${F[k].unit}</td><td class="num"><b>${fmt(sums[k],F[k].digits)}</b></td></tr>`).join('');document.getElementById('breakHead').innerHTML='<tr><th>Footing</th><th>Grid</th>'+fields.map(k=>`<th class="num">${F[k].label}<br><small>${F[k].unit}</small></th>`).join('')+'</tr>';document.getElementById('breakBody').innerHTML=ids.map(id=>{const r=rows[id];return `<tr><td>${r.structure_type}</td><td>${r.grid_ref}</td>${fields.map(k=>`<td class="num">${fmt(r[k],F[k].digits)}</td>`).join('')}</tr>`}).join('')}
function setOrder(mode){document.querySelectorAll('.orderCheck').forEach(cb=>cb.checked=mode==='ALL'||(mode!=='NONE'&&typeOf(cb.value)===mode));refreshOrder()}
function filterOrder(){const q=document.getElementById('orderSearch').value.toLowerCase();document.querySelectorAll('#orderList .checkrow').forEach(e=>e.style.display=e.dataset.search.includes(q)?'flex':'none')}
function toggleOrderMap(ev,el){ev.stopPropagation();const cb=document.querySelector(`.orderCheck[value="${el.dataset.id}"]`);if(cb){cb.checked=!cb.checked;refreshOrder()}}
function refreshOrder(){const ids=selected('.orderCheck');syncMap('orderMapPanel','#orderMapPanel',ids);document.getElementById('orderStatus').textContent=ids.length?`${ids.length} selected • spatial highlight active`:'0 selected';document.getElementById('orderCount').textContent=ids.length;document.getElementById('orderChips').innerHTML=ids.slice(0,12).map(id=>`<span class="chip">${rows[id].structure_type} · ${rows[id].grid_ref}</span>`).join('');const has=ids.length>0;document.getElementById('orderEmpty').style.display=has?'none':'block';document.getElementById('orderArea').style.display=has?'block':'none';if(!has){document.getElementById('orderRcc').textContent='0.000';return}const sum=k=>ids.reduce((a,id)=>a+Number(rows[id][k]),0), rcc=sum('drawing_rcc_volume_m3'),cem=sum('cement_bags'),sand=sum('sand_m3'),agg=sum('aggregate_m3');document.getElementById('orderRcc').textContent=fmt(rcc,3);document.getElementById('orderBody').innerHTML=`<tr><td>M20 RCC</td><td>m³</td><td class="num">${fmt(rcc,4)}</td></tr><tr><td>Cement</td><td>bags</td><td class="num">${fmt(cem,4)}</td></tr><tr><td>Sand</td><td>m³</td><td class="num">${fmt(sand,5)}</td></tr><tr><td>Aggregate</td><td>m³</td><td class="num">${fmt(agg,5)}</td></tr>`;document.getElementById('orderBreak').innerHTML=ids.map(id=>{const r=rows[id];return `<tr><td>${r.structure_type}</td><td>${r.grid_ref}</td><td class="num">${fmt(r.drawing_rcc_volume_m3,4)}</td><td class="num">${fmt(r.cement_bags,4)}</td><td class="num">${fmt(r.sand_m3,5)}</td><td class="num">${fmt(r.aggregate_m3,5)}</td></tr>`}).join('')}
document.addEventListener('click',e=>{if(!e.target.closest('#pop')&&!e.target.closest('.rccFoot'))closePop()});refreshSelector();refreshOrder();

function dIds(){return [...document.querySelectorAll('.structureCheck:checked')].map(x=>x.value)}
function dSync(){let ids=dIds();document.querySelectorAll('.detailCheck').forEach(c=>c.checked=ids.includes(c.value));}
function detailPick(c){let t=document.querySelector('.structureCheck[value="'+c.value+'"]');if(t)t.checked=c.checked;refreshSelector();}
function detailSet(on){document.querySelectorAll('.structureCheck').forEach(c=>c.checked=on);refreshSelector();}
function rccPlan(r){let max=Math.max(r.plan_length_m,r.plan_width_m),sx=235/max,w=r.plan_length_m*sx,h=r.plan_width_m*sx,x=(300-w)/2,y=(285-h)/2+12,uw=r.upper_plateau_length_m*sx,uh=r.upper_plateau_width_m*sx;return `<svg viewBox="0 0 300 320"><rect width="300" height="320" fill="white"/><text class="dtitle" x="10" y="18">Plan · drawing geometry</text><rect x="${x}" y="${y}" width="${w}" height="${h}" class="concreteDetail"/><rect x="${150-uw/2}" y="${y+h/2-uh/2}" width="${uw}" height="${uh}" fill="#cbd5e1" stroke="#64748b"/><text class="dsmall" x="10" y="296">Base ${r.plan_length_m.toFixed(3)} × ${r.plan_width_m.toFixed(3)} m</text><text class="dsmall" x="10" y="311">Upper plateau ${r.upper_plateau_length_m.toFixed(3)} × ${r.upper_plateau_width_m.toFixed(3)} m</text></svg>`}
function rccSection(r,trans){let L=trans?r.plan_width_m:r.plan_length_m,U=trans?r.upper_plateau_width_m:r.upper_plateau_length_m,x0=25,x1=275,yb=220,yt=60,edgeY=yb-r.edge_thickness_m/r.drawing_depth_m*(yb-yt),ratio=U/L,px0=150-(x1-x0)*ratio/2,px1=150+(x1-x0)*ratio/2,shape=`M ${x0} ${yb} L ${x0} ${edgeY} L ${px0} ${yt} L ${px1} ${yt} L ${x1} ${edgeY} L ${x1} ${yb} Z`,isCF=r.structure_type==='CF1',pcc=isCF?75:90;return `<svg viewBox="0 0 300 305"><rect width="300" height="305" fill="white"/><text class="dtitle" x="10" y="18">${trans?'Cross':'Longitudinal'} section · tapered RCC + stage context</text><rect x="${x0}" y="${yb+3}" width="${x1-x0}" height="${Math.max(5,pcc/10)}" fill="#a5f3fc" stroke="#0891b2"/><path d="${shape}" class="concreteDetail"/><rect x="134" y="28" width="32" height="32" fill="#e2e8f0" stroke="#94a3b8" stroke-dasharray="4 3"/><text class="dsmall" x="171" y="47">column/pedestal context</text><line x1="15" y1="${yb}" x2="285" y2="${yb}" class="dim"/><text class="dsmall" x="150" y="246" text-anchor="middle">${L.toFixed(3)} m base</text><line x1="285" y1="${yt}" x2="285" y2="${yb}" class="dim"/><text class="dsmall" x="288" y="145">${(r.drawing_depth_m*1000).toFixed(0)} mm</text><text class="dsmall" x="10" y="266">Edge thickness ${(r.edge_thickness_m*1000).toFixed(0)} mm · upper plateau ${U.toFixed(3)} m</text><text class="dsmall" x="10" y="281">PCC below is context (${pcc} mm); M40 authority is the gray M20 RCC body.</text><text class="dsmall" x="10" y="296">Current basis: ${r.current_basis}</text></svg>`}
function rccIso(r){let wide=Math.min(190,110+35*r.plan_length_m/r.plan_width_m),dx=55,topW=Math.min(wide*.8,wide*r.upper_plateau_length_m/r.plan_length_m);return `<svg viewBox="0 0 300 260"><rect width="300" height="260" fill="white"/><text class="dtitle" x="10" y="18">Mini isometric · RCC body</text><polygon points="40,175 ${40+wide},175 ${40+wide+dx},140 ${40+dx},140" fill="#cbd5e1" stroke="#64748b"/><polygon points="40,175 ${40+wide},175 ${40+wide},205 40,205" fill="#94a3b8" stroke="#64748b"/><polygon points="${150-topW/2},85 ${150+topW/2},85 ${150+topW/2+35},62 ${150-topW/2+35},62" fill="#e2e8f0" stroke="#64748b"/><line x1="${150-topW/2}" y1="85" x2="40" y2="175" stroke="#64748b"/><line x1="${150+topW/2}" y1="85" x2="${40+wide}" y2="175" stroke="#64748b"/><text class="dsmall" x="10" y="232">500 mm drawing-depth tapered working case.</text><text class="dsmall" x="10" y="247">2.5D aid; exact volume comes from frozen RCC DB.</text></svg>`}
function rccCard(r){return `<article class="detailCard"><div class="detailHead"><div><h3>${r.grid_ref} · ${r.structure_type}</h3><div style="font-size:12px;color:#64748b">${r.rcc_spec} · supports ${r.support_types.join(', ')}</div></div><div><span class="badge">${(r.drawing_depth_m*1000).toFixed(0)} mm depth</span><span class="badge">${r.drawing_rcc_volume_m3.toFixed(4)} m³</span></div></div><div class="detailViews"><div class="detailBox"><h4>A · Plan</h4>${rccPlan(r)}</div><div class="detailBox"><h4>B · Long section</h4>${rccSection(r,false)}</div><div class="detailBox"><h4>C · 2.5D</h4>${rccIso(r)}</div></div><div class="detailTable"><table><thead><tr><th>Item</th><th>Unit</th><th>Current quantity</th></tr></thead><tbody><tr><td>M20 RCC</td><td>m³</td><td>${r.drawing_rcc_volume_m3.toFixed(4)}</td></tr><tr><td>Cement</td><td>bags</td><td>${r.cement_bags.toFixed(4)}</td></tr><tr><td>Sand</td><td>m³</td><td>${r.sand_m3.toFixed(5)}</td></tr><tr><td>Aggregate</td><td>m³</td><td>${r.aggregate_m3.toFixed(5)}</td></tr></tbody></table></div><div class="detailNote"><b>Comparison only:</b> report-depth RCC ${r.report_comparison_rcc_volume_m3.toFixed(4)} m³ is not the current order total. Reinforcement belongs to V03/M50 and is not re-derived on this M40 page.</div></article>`}
function renderDetail(){dSync();let root=document.getElementById('detailCards');if(!root)return;let ids=dIds();root.innerHTML=ids.length?ids.map(id=>rccCard(rows[id])).join(''):'<div class="panel">Select one or more footings to inspect the source-backed concrete body and materials.</div>';}
const _m40RefreshSelector=refreshSelector;refreshSelector=function(){_m40RefreshSelector();renderDetail();};
renderDetail();


const CTX_GRID_LETTER_MM={A:0,B:2985,C:4900,D:5485,E:8748,F:9723,G:14688,H:19500,I:21000};
const CTX_GRID_NUMBER_MM={1:0,2:3600,3:6425,4:7080,5:10045,6:10700,7:12550};
const CTX_LEVELS=[{n:'Plinth',z:0},{n:'First floor',z:3048},{n:'Second floor',z:6096},{n:'Roof / total',z:9144}];
function ctxGridPoint(ref){
 ref=String(ref||'').trim().toUpperCase();
 let m=ref.match(/^([A-I])(\d)$/);if(m)return {x:CTX_GRID_LETTER_MM[m[1]],y:CTX_GRID_NUMBER_MM[Number(m[2])],letter:m[1],number:Number(m[2])};
 m=ref.match(/^([A-I])-([A-I])\/(\d)$/);if(m)return {x:(CTX_GRID_LETTER_MM[m[1]]+CTX_GRID_LETTER_MM[m[2]])/2,y:CTX_GRID_NUMBER_MM[Number(m[3])],letter:m[1]+'-'+m[2],number:Number(m[3])};
 return null;
}
function ctxAxisX(v,min,max){return 58+(v-min)/(max-min)*684}
function ctxY(z){const min=-2300,max=9500;return 452-(z-min)/(max-min)*392}
function ctxSafe(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function ctxStageMarkup(r,x,stage){
 let cf=String(r.structure_type||r.support_class||'').includes('CF1')||String(r.support_class||'').toLowerCase()==='cf1';
 let base=cf?-2015:-1990, top=base+500, out='';
 if(stage==='PCC'){
   let t=cf?75:90;out+=`<rect x="${x-16}" y="${ctxY(base)-3}" width="32" height="${Math.max(4,ctxY(base-t)-ctxY(base))}" fill="#0891b2" opacity=".88"/><text x="${x+20}" y="${ctxY(base-t)+10}" font-size="9" fill="#0e7490">PCC</text>`;
 }else if(stage==='V03'){
   out+=`<path d="M ${x-25} ${ctxY(base)} L ${x-25} ${ctxY(top-300)} L ${x-12} ${ctxY(top)} L ${x+12} ${ctxY(top)} L ${x+25} ${ctxY(top-300)} L ${x+25} ${ctxY(base)} Z" fill="#e2e8f0" stroke="#94a3b8"/>`;
   out+=`<line x1="${x-23}" y1="${ctxY(base+58)}" x2="${x+23}" y2="${ctxY(base+58)}" stroke="#2563eb" stroke-width="4"/><line x1="${x-17}" y1="${ctxY(top-58)}" x2="${x+17}" y2="${ctxY(top-58)}" stroke="#dc2626" stroke-width="4"/>`;
 }else if(stage==='M40'){
   out+=`<path d="M ${x-27} ${ctxY(base)} L ${x-27} ${ctxY(top-300)} L ${x-13} ${ctxY(top)} L ${x+13} ${ctxY(top)} L ${x+27} ${ctxY(top-300)} L ${x+27} ${ctxY(base)} Z" fill="#94a3b8" stroke="#475569" stroke-width="2"/>`;
   out+=`<rect x="${x-17}" y="${ctxY(base)-2}" width="34" height="4" fill="#0891b2" opacity=".55"/>`;
 }else if(stage==='M50'){
   out+=`<path d="M ${x-27} ${ctxY(base)} L ${x-27} ${ctxY(top-300)} L ${x-13} ${ctxY(top)} L ${x+13} ${ctxY(top)} L ${x+27} ${ctxY(top-300)} L ${x+27} ${ctxY(base)} Z" fill="#e2e8f0" stroke="#94a3b8"/>`;
   let dias=(r.longitudinal_pieces||[]).map(p=>Number(p.dia_mm)||0),dia=Math.max(12,...dias),lap=60*dia;
   let a=(Number(r.group_A_center_above_plinth_mm)||1321.5)+lap/2,b=(Number(r.group_B_center_above_plinth_mm)||4369.5)+lap/2;
   out+=`<line x1="${x-7}" y1="${ctxY(base)}" x2="${x-7}" y2="${ctxY(a)}" stroke="#2563eb" stroke-width="5"/><line x1="${x+7}" y1="${ctxY(base)}" x2="${x+7}" y2="${ctxY(b)}" stroke="#7c3aed" stroke-width="5"/>`;
   out+=`<rect x="${x-17}" y="${ctxY((Number(r.group_A_center_above_plinth_mm)||1321.5)+lap/2)}" width="34" height="${Math.max(5,ctxY((Number(r.group_A_center_above_plinth_mm)||1321.5)-lap/2)-ctxY((Number(r.group_A_center_above_plinth_mm)||1321.5)+lap/2))}" fill="#f59e0b" opacity=".16"/>`;
   out+=`<rect x="${x-17}" y="${ctxY((Number(r.group_B_center_above_plinth_mm)||4369.5)+lap/2)}" width="34" height="${Math.max(5,ctxY((Number(r.group_B_center_above_plinth_mm)||4369.5)-lap/2)-ctxY((Number(r.group_B_center_above_plinth_mm)||4369.5)+lap/2))}" fill="#f59e0b" opacity=".16"/>`;
 }
 return out;
}
function ctxElevationSvg(ids,axis,stage){
 let map=axis==='LETTER'?CTX_GRID_LETTER_MM:CTX_GRID_NUMBER_MM,vals=Object.values(map),min=Math.min(...vals),max=Math.max(...vals),keys=Object.keys(map),W=800,H=490;
 let s=`<svg viewBox="0 0 ${W} ${H}" role="img" aria-label="${axis==='LETTER'?'A to I':'1 to 7'} grid-linked project elevation"><rect width="${W}" height="${H}" fill="white"/><text x="15" y="22" font-size="14" font-weight="700" fill="#0f172a">${axis==='LETTER'?'A-I':'1-7'} grid-linked structural elevation</text><text x="15" y="40" font-size="10" fill="#64748b">Grid spacings are proportional to the Narayani structural plan; background datums are context, not a claim that every grid line carries a column.</text>`;
 CTX_LEVELS.forEach(l=>{let y=ctxY(l.z);s+=`<line x1="48" y1="${y}" x2="758" y2="${y}" stroke="#64748b" stroke-width="1.2"/><text x="8" y="${y+4}" font-size="9" fill="#475569">${l.n}</text><text x="762" y="${y+4}" font-size="9" fill="#64748b">${l.z} mm</text>`;});
 keys.forEach(k=>{let x=ctxAxisX(map[k],min,max);s+=`<line x1="${x}" y1="${ctxY(-2200)}" x2="${x}" y2="${ctxY(9300)}" stroke="#cbd5e1" stroke-width="1" stroke-dasharray="5 5"/><circle cx="${x}" cy="472" r="10" fill="white" stroke="#94a3b8"/><text x="${x}" y="475" text-anchor="middle" font-size="9" font-weight="700" fill="#475569">${k}</text>`;});
 let seen={};ids.forEach(id=>{let r=rows[id],p=ctxGridPoint(r&&r.grid_ref);if(!p)return;let v=axis==='LETTER'?p.x:p.y,x=ctxAxisX(v,min,max);let n=(seen[Math.round(x)]||0);seen[Math.round(x)]=n+1;x+=Math.min(n,4)*6-9;s+=ctxStageMarkup(r,x,stage);s+=`<text x="${x+10}" y="${ctxY(-2180)-n*11}" font-size="9" font-weight="700" fill="#0f172a">${ctxSafe(r.grid_ref)}</text>`;});
 s+=`<line x1="48" y1="${ctxY(-540)}" x2="758" y2="${ctxY(-540)}" stroke="#16a34a" stroke-dasharray="8 5"/><text x="52" y="${ctxY(-540)-5}" font-size="9" fill="#15803d">Ground/context datum (schematic)</text>`;
 s+=`</svg>`;return s;
}

function mountBackfillSpatialMaps(){
 const src=document.querySelector('#selectorMapPanel svg');if(!src)return;
 [['detailMapMount','detail-map-clone'],['contextMapMount','context-map-clone']].forEach(([mid,rid])=>{let m=document.getElementById(mid);if(!m||m.querySelector('svg'))return;let c=src.cloneNode(true);c.querySelectorAll('[id]').forEach(n=>n.removeAttribute('id'));c.setAttribute('id',rid);m.appendChild(c);});
}
function syncBackfillSpatialMaps(){
 let ids=dIds(),set=new Set(ids);
 ['detailSpatialMapPanel','contextSpatialMapPanel'].forEach(pid=>{let p=document.getElementById(pid);if(p)p.classList.toggle('has-selection',ids.length>0);});
 ['detailMapMount','contextMapMount'].forEach(mid=>{let m=document.getElementById(mid);if(!m)return;m.querySelectorAll('.rccFoot').forEach(e=>e.classList.toggle('selectedMap',set.has(e.dataset.id)));});
 let ds=document.getElementById('detailMapStatus'),cs=document.getElementById('contextMapStatus');if(ds)ds.textContent=ids.length?ids.length+' selected · click map or list to change':'0 selected';if(cs)cs.textContent=ids.length?ids.length+' selected · projected below':'0 selected';
}
function renderProjectContext(){let ids=dIds();let a=document.getElementById('ctxElevLetters'),b=document.getElementById('ctxElevNumbers');if(a)a.innerHTML=ctxElevationSvg(ids,'LETTER','M40');if(b)b.innerHTML=ctxElevationSvg(ids,'NUMBER','M40');let n=document.getElementById('contextStageNote');if(n)n.innerHTML="<b>M40 stage:</b> gray markers are the 500 mm drawing-basis tapered M20 footing RCC bodies. Cyan PCC and the vertical column/pedestal are shown only as neighboring-stage context. The report-depth 450 mm viewpoint remains comparison-only and does not replace the current M40 order basis.";}
mountBackfillSpatialMaps();
const _ctxRefresh=refreshSelector;refreshSelector=function(){_ctxRefresh();syncBackfillSpatialMaps();renderProjectContext();};
syncBackfillSpatialMaps();renderProjectContext();

